/*jslint plusplus: true, continue: true */
/*global REAL3D, THREE, console, document, window, $ */

REAL3D.InnerSpaceDesignEdit.WallData = {
    userPointTree: null,
    wallThick: null,
    wallHeight: null,
    drawObjects: null
};

REAL3D.InnerSpaceDesignEdit.WallData.init = function (wallData) {
    "use strict";
    unDrawData();
    unLoadData();
    if (wallData !== null) {
        
    }
    drawData();
};

REAL3D.InnerSpaceDesignEdit.WallData.drawData = function () {
    "use strict";
};

REAL3D.InnerSpaceDesignEdit.WallData.unDrawData = function () {
    "use strict";
}

REAL3D.InnerSpaceDesignEdit.WallData.loadData = function () {
    "use strict";
};

REAL3D.InnerSpaceDesignEdit.WallData.unLoadData = function () {
    "use strict";
};

REAL3D.InnerSpaceDesignEdit.WallData.saveData = function () {
    "use strict";
};
