/*jslint plusplus: true, continue: true */
/*global REAL3D, THREE, console */

REAL3D.MeshModel.Translate = function (elemIndex, mesh, elemType, deltaX, deltaY, deltaZ) {
    "use strict";
};

REAL3D.MeshModel.Rotate = function (elemIndex, mesh, elemType, deltaX, deltaY, deltaZ) {
    "use strict";
};

REAL3D.MeshModel.Scale = function (elemIndex, mesh, elemType, deltaX, deltaY, deltaZ) {
    "use strict";
};
