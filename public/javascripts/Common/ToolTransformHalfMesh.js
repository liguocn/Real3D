/*jslint plusplus: true, continue: true */
/*global REAL3D, THREE, console */

REAL3D.MeshModel.Translate = function (index, mesh, elemType, deltaX, deltaY, deltaZ) {
    "use strict";
};

REAL3D.MeshModel.Rotate = function (index, mesh, elemType, index, deltaX, deltaY, deltaZ) {
    "use strict";
};

REAL3D.MeshModel.Scale = function (inedex, mesh, elemType, deltaX, deltaY, deltaZ) {
    "use strict";
};
